<% layout("/layouts/boilerplate") %>
<body>
    <h3>All Listings</h3>
    <!-- <form method="get" action="/Lst/new">
        <button>Add New Listing</button>
    </form> -->
   
    <div class="row row-cols-lg-3 row-cols-md-2 row-cols-sm-1 row-cols-xsm-1">
        
        <% for(let listing of allListings) { %>
            <a href="/listings/<%=listing._id%>" class="listing-link">
    <div class="card col">
  <img src="<%=listing.image.url%>" class="card-img-top" alt="Listing_Image" height="220rem;">
  <div class="card-img-overlay"></div>
  <div class="card-body">
    <!-- <h5 class="card-title"><%=listing.title%></h5> -->
    <p class="card-text">
        <b><%=listing.title%></b>
        <br>
        Price: &#8377;<%= listing.price.toLocaleString("en-IN") %> / night</p>

    <!-- <a href="/listings/<%= listing._id%>" class="btn btn-primary">Show Detail</a> -->
  </div>
</div>
</a>
   <% }%>
    </div>
     
  
   
 
    </body>
